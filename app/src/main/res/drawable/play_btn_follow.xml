<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="false">
        <shape xmlns:android="http://schemas.android.com/apk/res/android">
            <stroke android:width="0.3dp" android:color="@color/app_color" /><!-- 描边，边框宽度、颜色 -->
            <solid android:color="@color/white" /><!-- 背景填充颜色 -->
        </shape>
    </item>
    <item android:drawable="@color/white" android:state_pressed="true">
        <shape xmlns:android="http://schemas.android.com/apk/res/android">
            <!-- 描边，边框宽度、颜色 -->
            <!--<stroke android:width="1dp" android:color="@color/gray_dd" />-->
            <solid android:color="@color/app_color" /><!-- 背景填充颜色 -->
        </shape>
    </item>

</selector>